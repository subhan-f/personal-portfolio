---
import MainLayout from '@layouts/MainLayout.astro';
import { fetchStrapi, getStrapiMedia } from '@lib/strapi';
import type { StrapiList, StrapiProject } from '@lib/strapi';

const { data: projects } = await fetchStrapi<StrapiList<StrapiProject>>('/projects', {
  'populate[image]': 'true',
  sort: 'publishedAt:desc',
});

const schema = {
  '@context': 'https://schema.org',
  '@type': 'CollectionPage',
  '@id': 'https://subhanfarrakh.com/projects',
  name: 'Project Archive',
  description: 'A collection of AI automation systems and full-stack applications by Subhan Farrakh.',
  url: 'https://subhanfarrakh.com/projects',
  author: { '@id': 'https://subhanfarrakh.com/#person' },
  mainEntity: {
    '@type': 'ItemList',
    numberOfItems: projects.length,
    itemListElement: projects.map((p, i) => ({
      '@type': 'ListItem',
      position: i + 1,
      name: p.title,
      url: `https://subhanfarrakh.com/projects/${p.slug}`,
    })),
  },
};
---

<MainLayout
  title="AI Automation & Web Development Projects | Subhan Farrakh"
  description="Explore real-world AI automation and full-stack projects by Subhan Farrakh."
>
  <Fragment slot="head">
    <script is:inline type="application/ld+json" set:html={JSON.stringify(schema)} />
  </Fragment>
  <section class="min-h-screen bg-black text-white py-20 px-4 md:px-8">
    <div class="max-w-7xl mx-auto">
      <h1 class="text-4xl md:text-5xl font-bold text-center mb-6">
        <span
          class="bg-linear-to-r from-[#1cd8d2] via-[#00bf8f] to-[#1cd8d2] bg-clip-text text-transparent"
        >
          Project Archive
        </span>
      </h1>
      <p class="text-gray-400 text-center max-w-2xl mx-auto mb-12">
        A collection of AI automation systems and full-stack applications I've built.
      </p>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        {
          projects.map((project) => (
            <a href={`/projects/${project.slug}`} class="block group h-full">
              <div class="relative overflow-hidden rounded-xl bg-gray-900/50 border border-white/10 h-full flex flex-col hover:border-[#1cd8d2]/50 hover:-translate-y-1 transition-all duration-300">
                <div class="relative h-48 overflow-hidden">
                  {project.image ? (
                    <img
                      src={getStrapiMedia(project.image.url)}
                      alt={project.image.alternativeText ?? project.title}
                      class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-105"
                      loading="lazy"
                      width="800"
                      height="450"
                    />
                  ) : (
                    <div
                      class="w-full h-full bg-gray-800"
                      style={`background-color: ${project.bgColor}`}
                    />
                  )}
                  <div
                    class="absolute inset-0 opacity-20 group-hover:opacity-0 transition-opacity"
                    style={`background-color: ${project.bgColor}`}
                  />
                </div>
                <div class="p-6 flex-1 flex flex-col">
                  <div class="flex items-center gap-2 text-xs text-gray-400 mb-2">
                    <span>{project.year}</span>
                  </div>
                  <h3 class="text-xl font-bold mb-2">{project.title}</h3>
                  <p class="text-gray-400 text-sm mb-4 flex-1">{project.shortDescription}</p>
                  <div class="flex flex-wrap gap-2">
                    {(project.technologies ?? []).slice(0, 3).map((tech) => (
                      <span class="text-xs px-3 py-1 bg-white/10 rounded-full text-gray-300">
                        {tech}
                      </span>
                    ))}
                    {(project.technologies ?? []).length > 3 && (
                      <span class="text-xs px-3 py-1 bg-white/10 rounded-full text-gray-300">
                        +{project.technologies.length - 3}
                      </span>
                    )}
                  </div>
                </div>
              </div>
            </a>
          ))
        }
      </div>
    </div>
  </section>
</MainLayout>
